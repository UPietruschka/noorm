## Velocity template to generate Java Bean classes
package $class.PackageName;

import java.io.Serializable;
import java.util.HashMap;
import org.noorm.jdbc.IBean;
import org.noorm.jdbc.JDBCColumn;

/**
 * Bean class for database table $class.TableName
 * DO NOT EDIT! This file has been auto-generated by NoORM
 */
public class $class.Name<T> implements IBean<T>, Serializable {

	private static final long serialVersionUID = $class.getSerialVersionUID()L;
	private static final String TABLE_NAME = "$class.TableName";
	private static final String[] PK_COLUMN_NAMES = new String[]{ #foreach ($param in $class.PrimaryKeyColumnNames)"$param"#if($velocityCount<$class.PrimaryKeyColumnNames.size()), #end#end };
	private static final String SEQUENCE_NAME = "$class.SequenceName";
	private static final String VERSION_COLUMN_NAME = "$class.VersionColumnName";
#foreach ($att in $class.Attributes)
	@JDBCColumn(name="$att.ColumnName", dataType="$att.DataType"#if(!$att.Updatable), updatable=false#end#if(!$att.Nullable), nullable=false#end#if($att.MaxLength>0), maxLength=$att.MaxLength#end)
	protected $att.Type $att.Name;
#end
	private T auxiliaryData;
#if ($class.enableOptLockFullRowCompare())
	private HashMap<String, Object> modifiedFieldsInitialValue = new HashMap<String, Object>();
#end

	public T getAuxiliaryData() {
		return auxiliaryData;
	}

	public void setAuxiliaryData(final T pAuxiliaryData) {
		auxiliaryData = pAuxiliaryData;
	}

	public HashMap<String, Object> getModifiedFieldsInitialValue() {
#if ($class.enableOptLockFullRowCompare())
		return modifiedFieldsInitialValue;
#else
		return null;
#end
	}

	public String getTableName() {
		return TABLE_NAME;
	}

	public String[] getPrimaryKeyColumnNames() {
		return PK_COLUMN_NAMES;
	}

	public String getSequenceName() {
		return SEQUENCE_NAME;
	}

	public String getVersionColumnName() {
		return VERSION_COLUMN_NAME;
	}
#foreach ($att in $class.Attributes)

	public $att.Type get$att.getMethodNamePostfix()() {
		return $att.Name;
	}

	public void set$att.getMethodNamePostfix()(final $att.Type p$att.getMethodNamePostfix()) {
#if ($class.enableOptLockFullRowCompare())
		if (!modifiedFieldsInitialValue.containsKey("$att.ColumnName")) {
			modifiedFieldsInitialValue.put("$att.ColumnName", $att.Name);
		}
#end
		$att.Name = p$att.getMethodNamePostfix();
	}
#end
#if ($class.hasPrimaryKey())
#if ($class.generatePKBasedEqualsAndHashCode())

	@Override
	public boolean equals(final Object pObject) {
		if (this == pObject) return true;
		if (pObject == null || !(pObject instanceof $class.Name)) return false;
		if (#foreach ($param in $class.PrimaryKeyJavaNames)get$param() == null#if($velocityCount<$class.PrimaryKeyJavaNames.size()) || #end#end) return false;
		final $class.Name other = ($class.Name) pObject;
		return #foreach ($param in $class.PrimaryKeyJavaNames)get$param().equals(other.get$param())#if($velocityCount<$class.PrimaryKeyJavaNames.size()) && #end#end;
	}

	@Override
	public int hashCode() {
		if (#foreach ($param in $class.PrimaryKeyJavaNames)get$param() == null#if($velocityCount<$class.PrimaryKeyJavaNames.size()) || #end#end) return super.hashCode();
		return #foreach ($param in $class.PrimaryKeyJavaNames)get$param().hashCode()#if($velocityCount<$class.PrimaryKeyJavaNames.size()) + #end#end;
	}
#end
#end
}